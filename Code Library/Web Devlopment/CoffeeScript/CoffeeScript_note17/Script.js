first.className = "text-black red" // insert class to HTML
// first.classList // this will run in console only and list out all the classes
first.classList.remove("text-black") // removes the class from the list(here removes means disables)
first.classList.add("text-black") // add the class to HTML
first.classList.toggle("red") // toggle add the class if absent and remove if present
first.classList.toggle("red") // This will help to understand
first.classList.contains("red") // check if list contain the specified class